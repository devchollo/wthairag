{
  "name": "wthairag",
  "version": "1.0.0",
  "description": "WorkToolsHub is a production-ready platform offering advanced public tools and a secure, private, multi-tenant AI workspace for RAG (Retrieval-Augmented Generation).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}