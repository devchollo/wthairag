{
    "buildCommand": "next build",
    "framework": "nextjs",
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/$1"
        }
    ]
}